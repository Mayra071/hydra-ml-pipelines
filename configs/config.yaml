defaults:
  - dataset: cal_housing
  - model: random_forest
  - training: base
  - preprocessing: standard
  - _self_

task_type: regression

project:
  name: Training_pipeline

mlflow:
  tracking_uri: mlruns
  experiment_name: training_pipeline


